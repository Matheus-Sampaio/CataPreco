{
  "name": "@catapreco/scraper",
  "version": "0.1.0",
  "description": "Marketplace scraping: pure extractors (JSON-LD, adapters, generic CSS) + fallback pipeline. I/O behind injectable ports.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@catapreco/core": "*",
    "cheerio": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^3.0.5"
  }
}
